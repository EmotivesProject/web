import React from 'react';
import { Segment } from 'semantic-ui-react';

const Post = ({ data }) => {
  console.log(data);

  return (
    <Segment>
      Feed
    </Segment>
  );
};

export default Post;
